import * as React from 'react';
import { App } from '@ohif/viewer';
// <App props={props} />
const OhifViewerV2 = (props) => {
  return (
    <App props={props} />
  );
};

export default OhifViewerV2;